---
'@lunaterra/ground-crew': minor
---

Expose a bounded Ground Crew HTTP rendering service that can be mounted in an existing application, and release pending jobs and request listeners during shutdown. The Docker docs demo uses this service behind a persistent, configurable Redis IP quota.
